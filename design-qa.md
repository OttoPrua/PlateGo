# PlateGo 规则卡整合设计质量检查

- 视觉依据：用户提供的规则卡参考截图，仅用于本地设计核对，未纳入仓库
- 实现截图：本轮已在内置浏览器完成共享“偏好预设”页面截图；真实扩展浮层仍待 Chrome 手动重新加载后捕获
- 视口：参考截图为 726 × 1074 像素；新版扩展需在 Chrome 重新加载后用同一 370 像素面板宽度捕获
- 显示密度校准：待捕获后记录
- 状态：随机选号页；号码推荐开启；三条示例规则 520、2233、沪114514；至少一条随机停用规则折叠

**已实现行为**

- 删除单独的“特定号码”复选框、文本框和组合标签；旧自定义组合首次升级时迁移为顺序规则。
- 规则卡标题直接显示规则内容；固定模式显示“沪”和位数，顺序模式隐藏“沪”与位数标签。
- 随机高亮和自编筛选共用规则内容，并分别保存启用状态；当前页面停用的规则折叠。
- 首次升级加入顺序规则 520、2233 和限定位置规则 114514，分别覆盖燃油车与新能源长度。
- 号码推荐将旧“连号”扩展为“相同数字”：同一数字出现至少两次且不要求相邻；可指定只高亮哪些数字，留空时匹配全部。
- “好多数”保留同一数字至少三次的橙色高亮，也可指定数字；相同数字与好多数同时命中时，好多数视觉优先。
- 顺序号从两个相邻连续数字开始识别；相同数字、顺序号和好多数都可选填目标，留空时自动高亮全部符合项。
- 规则卡顶部固定为“随机高亮/自编筛选、限定位置/匹配顺序、删除”三列，切换状态不再左右跳动。
- 基本信息、确认信息等选号前页面新增“选号规则预置”，可提前切换并设置随机或自编启用状态。
- 随机页规则只高亮，不提供选号或提交动作。

**自动验证**

- TypeScript 类型检查：通过。
- JavaScript 语法检查：通过。
- 单元测试：46/46 通过。
- 插件契约测试：38/38 通过。
- 网页与插件构建：通过。
- MV3 插件校验：通过。

**对比历史**

- 第 1 轮：按参考图保留原有绿色卡片系统，完成规则整合、上下文独立开关、停用折叠和好多数高亮。
- 第 2 轮：尝试在用户 Chrome 中打开新版本地样机；Chrome 外部控制不能接管 `chrome://extensions`，且本地预览地址被浏览器控制层拦截，因此无法在本轮重新加载扩展并生成真实实现截图。
- 第 3 轮：用隔离临时 Chrome 配置启动本地样机，但当前 Chrome 忽略命令行加载的未打包扩展，调试目标中没有 PlateGo 扩展后台进程；未触碰用户的官方选号标签页，也未生成实现截图。
- 第 4 轮：在内置浏览器打开本次构建的共享偏好预设页面，确认三个可选目标输入框均可见；“相同数字”输入 `8896` 后即时归一化为 `896`，清空后恢复“匹配全部”。扩展工作台直接用普通网页打开时因缺少 Chrome 本地存储接口不可渲染，因此真实浮层仍以手动重载后的 Chrome 为准。

**重新加载后的视觉检查清单**

- 规则卡标题是否完整显示 520、2233、沪114514，长标题无溢出。
- 顺序模式是否完全隐藏“沪”和 1–6/1–7 位数字标签。
- 关闭“随机高亮”或“自编筛选”后，卡片是否缩为单行且仍可恢复。
- 好多数与相同数字同时命中时，橙色好多数外框是否清楚覆盖黄色相同数字外框。
- 三条默认规则在 370px 面板内是否无需横向滚动。
- 三个规则卡操作按钮是否纵向对齐，切换启用状态或匹配模式时位置稳定。
- 相同数字填 `8、9`、顺序号填 `12、56`、好多数填 `8、9` 后是否只高亮对应目标；清空后是否恢复自动高亮全部。
- 进入选号前能否看到“选号规则预置”，切换随机/自编后启用状态是否分别保留。
- 控制台是否无错误。

最终结果：自动检查通过；真实页面验收需在 `chrome://extensions` 重新加载扩展并刷新选号页后进行。
