# 更新日志

## v0.1.0（2026-08-31）

PlateGo 的首个公开测试版本，以上海电脑端选号流程为首个适配目标。

### 主要功能

- 提供可独立运行的 Chrome MV3 选号助手和本地网页样机；无需私有后端即可使用固定模拟数据。
- 兼容已精确识别的上海官方模拟选号和正式选号页面，支持本机只读号码、规则高亮、自编号池筛选和按组填入。
- 支持限定位置与匹配顺序规则；匹配顺序会保留模板内部空位作为单字符通配位，并允许整段出现在号码任意位置。
- 自编候选按每组 5 个管理，可拖动排序；提交后未选中的上一轮号码会灰色排除，避免重复消耗机会。
- 随机选号支持相同数字、好多数、顺序号、数字回环和特定位置规则的分级描边与静态泛光。
- 支持把合格证识别结果和车辆资料保存在浏览器本地，并辅助填入确认信息；不会替用户确认或提交。
- 号池、规则、选择顺序、车辆资料和页面采集默认只保存在本机。

### 安全边界

- 插件不会点击验证、确认选号、换批或提交按钮。
- 未识别的官方路由保持安全关闭。
- 私有后端、购买校验、官方页面采集、用户车辆资料和合格证不包含在公开版本中。

### 验证结果

- 46 个单元测试通过。
- 38 个插件契约测试通过。
- TypeScript 类型检查、网页与插件生产构建、MV3 插件校验全部通过。
